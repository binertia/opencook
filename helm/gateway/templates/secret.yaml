apiVersion: v1
kind: Secret
metadata:
  name: {{ include "gateway.fullname" . }}-secrets
  labels:
    {{- include "gateway.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
