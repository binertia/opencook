# TASK-0040: Implement Cache Metrics and Analytics

## Objective
Build cache metrics collection (hit rate by layer, by model) and analytics storage in the cache_metadata table.

## Context
Cache metrics enable monitoring and optimization. The `cache_metadata` table tracks cache entry metadata for analytics. See CACHE.md section 7 and DATABASE.md section 2.14.

## Acceptance Criteria
- [x] `crates/gateway-cache/src/metrics.rs` — cache metrics
  - `cache_hit_l1_total` counter — L1 hits
  - `cache_hit_l2_total` counter — L2 hits
  - `cache_miss_total` counter — misses
  - `cache_hit_by_model_total` counter — per-model breakdown
  - `cache_cost_saved_total` counter — estimated cost savings
- [x] Metrics emitted via Prometheus-compatible format at `/metrics`
- [x] Cache metadata stored in `cache_metadata` table on cache insert:
  - cache_key_hash, model_id, prompt_preview (first 200 chars), prompt_tokens
  - ttl_seconds, expires_at, hit_count (incremented on each hit)
- [x] `crates/gateway-cache/src/analytics.rs` — cache analytics queries
  - `get_hit_rate(org_id, period) -> f64`
  - `get_cost_saved(org_id, period) -> f64`
  - `get_top_cached_models(org_id, limit) -> Vec<ModelCacheStats>`
- [x] Admin API: `GET /api/v1/cache/stats` — cache statistics

## Files Affected
- `crates/gateway-cache/src/metrics.rs` — cache metrics facade (created)
- `crates/gateway-cache/src/analytics.rs` — analytics queries (created)
- `crates/gateway-cache/src/lib.rs` — module exports
- `crates/gateway-cache/src/two_tier.rs` — wired metrics into get()
- `crates/gateway-cache/Cargo.toml` — added gateway-db, gateway-observability, sqlx deps
- `crates/gateway-db/src/models.rs` — added `CacheMetadata` model
- `crates/gateway-db/src/repos/cache_meta_repo.rs` — cache_metadata CRUD (created)
- `crates/gateway-db/src/repos/mod.rs` — added cache_meta_repo module
- `crates/gateway-observability/src/metrics.rs` — added `record_cache_hit_by_model` and `record_cache_cost_saved`
- `crates/gateway-api/src/routes/cache.rs` — cache stats endpoint (created)
- `crates/gateway-api/src/routes/mod.rs` — added cache module
- `crates/gateway-api/src/router.rs` — registered `/api/v1/cache/stats`
- `crates/gateway-api/src/routes/chat.rs` — wired cache metadata recording on hit/insert

## Dependencies
- TASK-0039 (cache integration — to track hits)
- TASK-0010 (cache_metadata table migration)

## Technical Notes
- Uses `metrics` crate for counters: `gateway_cache_hit_total` with `layer` label
- `gateway_cache_hit_by_model_total` with `model` label
- `gateway_cache_cost_saved_total` in micro-dollars
- Cache metadata upsert on insert, hit_count increment on hits
- Fire-and-forget DB updates (non-blocking)
- Analytics queries use `usage_records` table for cost data
- Cost saved = total_cost × hit_rate / (1 - hit_rate)

## Test Requirements
- [x] Unit test: hit rate calculation (hits / total)
- [x] Unit test: cost saved calculation
- [ ] Integration test: cache hit increments correct counters
- [ ] Integration test: cache metadata stored on insert
- [ ] Integration test: cache stats endpoint returns correct data
