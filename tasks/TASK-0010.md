# TASK-0010: Create Usage, Quota, Webhook, and Audit Migrations (0010-0017)

## Objective
Write migrations 0010-0017 for usage_records, quotas, quota_usage, webhooks, webhook_deliveries, cache_metadata, audit_log, and sessions tables.

## Context
These tables support billing aggregation, quota enforcement, webhook delivery, cache analytics, audit trails, and dashboard sessions. See DATABASE.md sections 2.9-2.16.

## Acceptance Criteria
- [ ] Migration `0010_create_usage_records.sql` — partitioned by period_start, unique constraint on aggregation key
- [ ] Migration `0011_create_quotas.sql` — with quota_period and quota_metric enums
- [ ] Migration `0012_create_quota_usage.sql` — with unique constraint on quota window
- [ ] Migration `0013_create_webhooks.sql` — with webhook_event enum
- [ ] Migration `0014_create_webhook_deliveries.sql` — partitioned by created_at
- [ ] Migration `0015_create_cache_metadata.sql` — cache analytics tracking
- [ ] Migration `0016_create_audit_log.sql` — partitioned by created_at, immutable (no updated_at)
- [ ] Migration `0017_create_sessions.sql` — session token storage with expiry
- [ ] All 8 migrations apply cleanly in sequence

## Files Affected
- `migrations/0010_create_usage_records.sql` — new
- `migrations/0011_create_quotas.sql` — new
- `migrations/0012_create_quota_usage.sql` — new
- `migrations/0013_create_webhooks.sql` — new
- `migrations/0014_create_webhook_deliveries.sql` — new
- `migrations/0015_create_cache_metadata.sql` — new
- `migrations/0016_create_audit_log.sql` — new
- `migrations/0017_create_sessions.sql` — new

## Dependencies
- TASK-0009 (routing_rules, requests, responses migrations)

## Technical Notes
- `quota_period` enum: minute, hour, day, month, total
- `quota_metric` enum: requests, tokens, cost_usd
- `webhook_event` enum: request.completed, request.failed, quota.warning, quota.exceeded, provider.error, provider.recovered
- `audit_action` enum: create, update, delete, login, logout, api_key.created, api_key.revoked, provider.created, provider.updated, provider.deleted, quota.exceeded, quota.warning, webhook.created, webhook.deleted, routing_rule.created, routing_rule.updated, settings.updated, billing.updated
- audit_log has NO `updated_at` column — it is append-only and immutable

## Test Requirements
- All 8 migrations apply in sequence
- Verify each table has correct columns, indexes, and constraints
- Verify audit_log has no updated_at column or trigger
- Verify unique constraints on usage_records and quota_usage
