# TASK-0071: Add Semantic Cache Configuration UI

## Objective
Add semantic cache configuration to the admin dashboard with similarity threshold and status toggles.

## Context
Admins need to enable/disable semantic caching and tune the similarity threshold. See EPICS.md Epic-14.

## Acceptance Criteria
- [ ] `frontend/src/components/settings/SemanticCacheSettings.tsx` — settings component
  - Enable/disable toggle for semantic caching
  - Similarity threshold slider: 0.90 to 0.99 (default 0.97)
  - Embedding model selector (text-embedding-3-small only for now)
  - Max entries per org input
  - Save button → PUT /api/v1/organizations/{org_id}/settings
- [ ] Display semantic cache stats: hit rate, total entries, cost saved
- [ ] Show warning: "Semantic cache requires additional embedding API calls"
- [ ] Settings stored in `organizations.settings` JSONB field

## Files Affected
- `frontend/src/components/settings/SemanticCacheSettings.tsx` — new
- `frontend/src/pages/settings/OrganizationSettings.tsx` — updated with semantic tab

## Dependencies
- TASK-0068 (semantic cache backend)
- TASK-0049 (organization settings page)

## Technical Notes
- Use shadcn/ui `Switch` for enable/disable
- Use shadcn/ui `Slider` for threshold (step 0.01)
- Stats from `GET /api/v1/cache/semantic-stats`
- Warning about embedding costs: approximate cost per cached query
- Use `react-hook-form` for form management

## Test Requirements
- Toggle enables/disables semantic cache
- Threshold slider updates correctly
- Save persists settings
- Stats display correctly
