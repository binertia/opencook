# TASK-0009: Create Routing Rules, Requests, and Responses Migrations (0007-0009)

## Objective
Write migrations 0007-0009 for routing_rules, requests (partitioned), and responses (partitioned) tables.

## Context
Routing rules define provider selection strategies. Requests and responses are high-write partitioned tables. See DATABASE.md sections 2.6-2.8 and 4.

## Acceptance Criteria
- [ ] Migration `0007_create_routing_rules.sql` creates routing_rules with routing_strategy enum, indexes (uk_routing_rules_org_name, idx_routing_rules_org_priority, idx_routing_rules_match_model, idx_routing_rules_conditions GIN), and trigger
- [ ] Migration `0008_create_requests_partitioned.sql` creates requests table partitioned by RANGE on created_at, with all columns, indexes, and request_status enum
- [ ] Migration `0009_create_responses_partitioned.sql` creates responses table partitioned by RANGE on created_at, with all columns and indexes
- [ ] Initial monthly partitions created for current month and next month in migrations 0008 and 0009
- [ ] All migrations apply cleanly in sequence

## Files Affected
- `migrations/0007_create_routing_rules.sql` — new
- `migrations/0008_create_requests_partitioned.sql` — new
- `migrations/0009_create_responses_partitioned.sql` — new

## Dependencies
- TASK-0008 (api_keys, provider_configs, provider_models must exist for FK references)

## Technical Notes
- `routing_strategy` enum: fallback, weighted, conditional, single
- `request_status` enum: pending, processing, success, error, timeout, cancelled
- `conditions` and `targets` in routing_rules are JSONB columns
- Partition naming convention: `{table}_yYYYYmMM`
- Include `request_body_truncated` and `response_body_truncated` boolean flags

## Test Requirements
- All migrations apply in sequence on database with 0001-0006 applied
- Verify partitions exist: `SELECT inhrelid::regclass FROM pg_inherits`
- Verify request_status enum values cannot accept invalid input
