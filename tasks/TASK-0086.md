# TASK-0086: Implement CORS, CSRF Protection, and Security Audit

## Objective
Finalize CORS configuration, add CSRF protection for admin endpoints, and run a comprehensive security audit.

## Context
CORS and CSRF protect against cross-origin attacks. The security audit identifies and fixes any remaining vulnerabilities. See AUTH.md section 7 and EPICS.md Epic-17.

## Acceptance Criteria
- [x] CORS finalization:
  - `GATEWAY_ALLOWED_ORIGINS` parsed from env var
  - Wildcard `*` not allowed in production (must specify origins)
  - Credentials allowed for admin API (cookies)
  - Preflight caching: `Access-Control-Max-Age: 86400`
- [x] CSRF protection for admin API:
  - Double-submit cookie pattern for state-changing requests
  - `X-CSRF-Token` header required for POST/PUT/DELETE to `/api/v1/*`
  - Token generated on login, stored in `SameSite=Strict` cookie
- [x] Security audit checklist:
  - `cargo audit` run; `idna` RUSTSEC-2024-0421 fixed by upgrading `validator` → 0.20
  - No hardcoded secrets in codebase
  - No stack traces in error responses
  - Input validation on all endpoints
  - Rate limiting on all public endpoints
- [x] `security/audit-checklist.md` — documented security checklist
  - OWASP Top 10 mitigations mapped to gateway features
  - Secure deployment guidelines
  - Dependency audit results and accepted risks
- [x] Static analysis: `cargo clippy` with `-W clippy::unwrap_used -W clippy::expect_used` run

## Files Affected
- `Cargo.toml` — `validator` 0.20, `reqwest` `cookies` feature, `tower-cookies`
- `crates/gateway-api/src/state.rs` — `environment` config field
- `crates/gateway-api/src/router.rs` — CORS hardening, CSRF middleware registration, `CookieManagerLayer`
- `crates/gateway-api/src/middleware/csrf.rs` — CSRF protection middleware
- `crates/gateway-api/src/middleware/mod.rs` — register CSRF module
- `crates/gateway-api/src/routes/auth.rs` — return `csrf_token` on login/refresh, set cookie
- `crates/gateway-api/tests/helpers/test_app.rs` — cookie-enabled reqwest client, `environment` field
- `crates/gateway-api/tests/e2e_csrf.rs` — CSRF integration tests
- `security/audit-checklist.md` — security documentation

## Dependencies
- TASK-0082 (input validation)
- TASK-0083 (TLS and security headers)

## Technical Notes
- CSRF token: 32 random bytes hex-encoded, generated on login/refresh
- SameSite=Strict on CSRF cookie; Secure flag when TLS is configured
- CORS: wildcard blocked in `production` environment; explicit origins required
- `cargo audit` findings:
  - Fixed: `idna` 0.5.0 (RUSTSEC-2024-0421) via `validator` upgrade
  - Accepted: `rsa` 0.9.10 (RUSTSEC-2023-0071) — transitive via `sqlx-mysql`, unused
  - Accepted: `paste` 1.0.15 unmaintained — `ratatui` dependency
  - Accepted: `rustls-pemfile` 2.2.0 unmaintained — migration deferred

## Test Requirements
- [x] Integration test: CSRF token missing → 403 for POST
- [x] Integration test: CSRF token present → request succeeds
- [x] Integration test: CSRF skipped for GET requests
- [x] Integration test: login returns `csrf_token` in body and cookie
