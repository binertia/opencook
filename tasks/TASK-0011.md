# TASK-0011: Create Indexes, Triggers, RLS Policies, and Seed Data Migrations (0018-0022)

## Objective
Write the final migrations 0018-0022: all remaining indexes, updated_at triggers applied to all mutable tables, row-level security policies, initial partitions, and seed data.

## Context
This is the final database setup step. RLS policies provide defense-in-depth for tenant isolation. Triggers auto-update `updated_at`. Initial partitions ensure tables are ready for writes. See DATABASE.md sections 3, 5, and 6.

## Acceptance Criteria
- [ ] Migration `0018_create_indexes.sql` — creates any remaining indexes not in table migrations
- [ ] Migration `0019_create_triggers.sql` — `fn_update_timestamp()` applied to all mutable tables (excludes audit_log)
- [ ] Migration `0020_create_rls_policies.sql` — RLS enabled and policies created for all tenant tables
- [ ] Migration `0021_create_partitions.sql` — creates initial monthly partitions for current month + 1 month ahead
- [ ] Migration `0022_seed_default_data.sql` — seeds system organization and any enum defaults
- [ ] Full migration sequence `0001-0022` runs cleanly on fresh database
- [ ] `EXPLAIN ANALYZE` on hot-path queries shows index usage (quota check, routing lookup, request log)

## Files Affected
- `migrations/0018_create_indexes.sql` — new
- `migrations/0019_create_triggers.sql` — new
- `migrations/0020_create_rls_policies.sql` — new
- `migrations/0021_create_partitions.sql` — new
- `migrations/0022_seed_default_data.sql` — new

## Dependencies
- TASK-0010 (all table migrations 0010-0017)

## Technical Notes
- RLS policy template: `CREATE POLICY tenant_isolation_{table} ON {table} USING (org_id = current_setting('app.org_id')::UUID)`
- Triggers: 14 tables get updated_at triggers (all except audit_log)
- Partition creation: use dynamic SQL to create partitions for current month and next month
- Seed data: insert system organization with id `'00000000-0000-0000-0000-000000000001'`

## Test Requirements
- Full migration run `sqlx migrate run` on fresh database completes in < 30 seconds
- Verify RLS: attempt query from unauthorized session is blocked
- Verify trigger: UPDATE changes updated_at automatically
- Verify indexes used via EXPLAIN on: quota check query, routing rule lookup, request log by org
