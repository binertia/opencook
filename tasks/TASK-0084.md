# TASK-0084: Implement Audit Log System

## Objective
Build the immutable audit log that records all security-relevant actions with actor attribution and tamper-proof design.

## Context
Audit logs are append-only records of security-relevant actions. They cannot be modified or deleted. See DATABASE.md section 2.15 and EPICS.md Epic-17.

## Acceptance Criteria
- [x] `crates/gateway-db/src/repos/audit_repo.rs` — audit log repository
  - `record(action, actor_id, org_id, resource_type, resource_id, details)` — inserts audit entry
  - `list(org_id, filters) -> Vec<AuditEntry>` — query with filtering
  - No update or delete methods (immutable)
- [x] Audit log table (`audit_log`) has NO `updated_at` column
  - Insert-only: no UPDATE or DELETE operations allowed
  - PostgreSQL migration uses monthly partitions (S3 archiving deferred)
  - 90-day retention configurable (enforcement deferred to background worker)
- [x] Tracked actions:
  - `api_key.created`, `api_key.revoked`
  - `provider.created`, `provider.updated`, `provider.deleted`
  - `routing_rule.created`, `routing_rule.updated`
  - `quota.exceeded`, `quota.warning`
  - `webhook.created`, `webhook.deleted`
  - `settings.updated`, `billing.updated`
  - `login`, `logout`, `user.role_changed`
  - `security.violation` (cross-org access attempts)
- [x] `GET /api/v1/organizations/{org_id}/audit-log` — paginated audit log
  - Filters: action, actor (user/api-key), date range, resource type, entity ID
- [x] Admin API with RBAC (owner/admin only)
  - `AuditRead` permission restricted to Owner and Admin roles
  - Cross-org access returns 403

## Files Affected
- `crates/gateway-db/src/pool.rs` — SQLite `audit_log` schema
- `crates/gateway-db/src/models.rs` — `AuditEntry`, `AuditAction` types
- `crates/gateway-db/src/lib.rs` — re-exports
- `crates/gateway-db/src/repos/audit_repo.rs` — new repository
- `crates/gateway-db/src/repos/mod.rs` — register repository
- `crates/gateway-auth/src/rbac.rs` — restrict `AuditRead` to Owner/Admin
- `crates/gateway-api/src/audit.rs` — audit recording helpers
- `crates/gateway-api/src/middleware/audit_context.rs` — inject `AuditRequestContext`
- `crates/gateway-api/src/middleware/mod.rs` — register middleware
- `crates/gateway-api/src/router.rs` — wire audit routes and middleware
- `crates/gateway-api/src/routes/audit.rs` — audit log endpoints
- `crates/gateway-api/src/routes/auth.rs` — record login events
- `crates/gateway-api/src/routes/api_keys.rs` — record key lifecycle events
- `crates/gateway-api/src/routes/providers.rs` — record provider lifecycle events
- `crates/gateway-api/src/routes/users.rs` — record user lifecycle and role changes
- `crates/gateway-api/src/routes/webhooks.rs` — record webhook lifecycle events
- `crates/gateway-api/src/routes/quotas.rs` — record quota lifecycle events
- `crates/gateway-api/tests/e2e_audit.rs` — integration tests

## Dependencies
- TASK-0010 (audit_log table migration)
- TASK-0016 (RBAC for access control)

## Technical Notes
- No `updated_at` trigger on audit_log (immutable)
- Partitioning: monthly in PostgreSQL, auto-create next month (handled by migration `0016`)
- Retention: 90 days, then archive (S3 integration deferred)
- Use `INSERT ONLY` — no application-level update/delete
- Details stored as JSON in SQLite and JSONB in PostgreSQL
- Actor ID from AuthContext (`user_id` or `key_id`)
- `AuditRequestContext` middleware extracts IP, User-Agent, and Request-ID for every request

## Test Requirements
- [x] Integration test: audit entry created on API key creation
- [x] Integration test: audit entry created on provider update
- [x] Integration test: audit log endpoint returns entries
- [x] Integration test: RBAC denies viewers
- [x] Integration test: cross-org access attempt denied with 403
