
-- Partitions are dropped automatically when parent table is dropped;
-- explicit cleanup here is optional and complex. We leave partitions in place
-- for safety; they can be removed manually if needed.

